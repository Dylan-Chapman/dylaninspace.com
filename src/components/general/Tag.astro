---
const { tag } = Astro.props;
---

{
  tag && (
    <span class="group text-xs bg-transparent border-cosmos-secondary border-[1px] leading-none rounded-full flex flex-row content-center items-center h-[34px] px-3 text-cosmos-secondary">
      <span>{tag}</span>
    </span>
  )
}
